---
needsTranslation: true
title: acos
module: Math
submodule: Trigonometry
file: src/math/trigonometry.js
description: |
  <p>Calculates the arc cosine of a number.</p>
  <p><code>acos()</code> is the inverse of <a href="/reference/p5/cos/">cos()</a>. It expects
  arguments in the range -1 to 1. By default, <code>acos()</code> returns values in the
  range 0 to π (about 3.14). If the
  <a href="/reference/p5/angleMode/">angleMode()</a> is <code>DEGREES</code>, then values are
  returned in the range 0 to 180.</p>
---
<!--
  Auto-generated translation stub (es).
  Translate from the English source and remove needsTranslation when done.
  English source: src/content/reference/en/p5 copy/acos.mdx
-->

<!-- Translation needed. Replace this placeholder with the translated content. -->
